# Standard telemetry

Three live refreshes per second with continuous, one-second coordinator/process-tree resource samples.
Set `resource_sampling: off` in a typed telemetry override only when resource monitoring is intentionally
disabled for a controlled comparison.
