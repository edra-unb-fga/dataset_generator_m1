# Realistic heavy appearance

Reviewed standard appearance using plausible blur, noise, lighting, shadow, flare, temperature, and light drizzle. It is intentionally slower than `current-fast` and remains the default.
